# PostgreSQL Database Configuration
# IMPORTANT: Replace these values with your actual PostgreSQL credentials from DBeaver

DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_actual_database_name
DB_USER=your_actual_postgres_user
DB_PASSWORD=your_actual_postgres_password

# Table name (this should match your table in DBeaver)
TABLE_NAME=newtable
